Домашнее задание от 13.03.2017
Группа: 13.02_FSTK_Пн(19:00-21:00)_Сб(10:00-14:00)_
Преподаватель: Ивченко Дмитрий Frontend

Что задано домой?

***1) Написать функцию реверс масива при помощи деления масива и дополнительной переменной (или без нее=)))
2) Переписать калькуляты про помощи callbackFunctions


3) В аттаче код, который надо дописать так чтобы у пользоателя цыклически спрашивалось
хочет ли он ввесте еще пользователей. И пользователя сохранялись в масив объектов.
Если пользователь выбирает ввести нового, то в  вызывается фукнция заполнения
Если позьзователь выбирает посмотреть таблицу, то весь масив генерится в таблицу
( подобно тому как мы на паре делали со списком)
        var persons = [];
        var person = {
            name: '',
            sname: '',
            age: ''
        };
        function ask() {
            person.name = prompt("Введите свое имя:");
            person.sname = prompt("Введите свою фамилию:");
            person.age = prompt("Введите свой возраст:");
            persons.push(person);
        }
        function say() {
            document.write("Имя: " + person.name + " ");
            document.write("Фамилия: " + person.sname + "");
            document.write("Возраст: " + person.age + "");
            document.write("");
        }

        ask();
        say();